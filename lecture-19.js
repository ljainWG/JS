// callback queue, microtask queue, eventloop

// call back function which can come inside micro task queue are from : promises and mutation observer

// starvation of tasks inside the callback queue

// what is mutation observer, dom tree

// web apis
// setTimeout, dom apis, fetch(), local storage, console, location, history, indexedDB, web workers, web sockets, service workers, etc.

// we get accesss to all these web apis in the browser environment to js by global object called window

// browser
// Javascript runtime enviroment <=> js engine
// js engine, apis, event loop, call back queue, micro task queue
// ecma script
// js is interpretter or compiler lang ? well it depends on the js engine which is being used
// jit(just-in-time) compilation
// aot(ahead-of-time) compilation
// heap memory and call stack, garbage collector
// mark and sweep algorithm
// inlining, copy elision, inline caching
// ignition, turbofan and orinoco, oilpan in google v8 engine